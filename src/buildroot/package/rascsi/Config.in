config BR2_PACKAGE_RASCSI
	bool "rascsi_enabled"
	select BR2_PACKAGE_SPDLOG
	select BR2_PACKAGE_ZLIB
	select BR2_INSTALL_LIBSTDCPP
	help
	  This package builds the RaSCSI service and includes
	  it in the generated file system image.
	
	  http://www.rascsi.com

config BR_PACKAGE_RASCSI_OLED
	bool "rascsi_oled"
	select BR2_PACKAGE_PYTHON3
	help
	  This includes the python script that will update the
	  optional attached OLED display

	  http://www.rascsi.com
